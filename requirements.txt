# Core video processing and VOID package export
opencv-python>=4.8,<5
numpy>=1.24,<3
tqdm>=4.66,<5
pillow>=9.4,<12
scipy>=1.10,<2

# FastAPI UI, uploads, and notebook-launched server
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1

# Redis/Celery job queue used by the UI processing API
celery[redis]>=5.3,<6
redis>=5,<6

# SAM2/SAMURAI/DAM4SAM/SAM2Long tracking backends
# Colab L4 should install Torch from the active Colab CUDA index/runtime.
torch>=2.5.1
torchvision>=0.20.1
hydra-core>=1.3.2,<2
omegaconf>=2.3,<3
iopath>=0.1.10,<1

# Research tracker adapter support
vot-toolkit==0.7.1
vot-trax==4.0.2
loguru>=0.7,<1

# Optional legacy AI inpaint engine exposed in the UI as "AI LaMa"
simple-lama-inpainting>=0.1.2

# VOID Pass 1 itself is installed from /content/void-model/requirements.txt
# by in_colab.ipynb, then supplemented there with:
# huggingface_hub, accelerate, safetensors
